import React from 'react';
import { Box, Typography } from '@mui/material';
import styles from './page.module.css';

const Header = () => {
  return (
    <Box className={styles.headerContainer}>
        <p>(imagem hipotética)</p>
    </Box>
  );
};

export default Header;
